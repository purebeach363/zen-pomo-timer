<Window x:Class="zen_pomo_timer.Views.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:zen_pomo_timer.Views"
        mc:Ignorable="d"
        Title="Statistics" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/Icons/stats.ico"
        Background="#333333">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Summary Cards -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Border Background="#4A148C" Padding="20,10" CornerRadius="8" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊 " FontSize="24"/>
                    <StackPanel>
                        <TextBlock Text="Total Pomodoros" Foreground="#AAAAAA" FontSize="12"/>
                        <TextBlock x:Name="txtTotalPomodoros" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="#4A148C" Padding="20,10" CornerRadius="8" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏱️ " FontSize="24"/>
                    <StackPanel>
                        <TextBlock Text="Total Minutes" Foreground="#AAAAAA" FontSize="12"/>
                        <TextBlock x:Name="txtTotalMinutes" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="#4A148C" Padding="20,10" CornerRadius="8" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 " FontSize="24"/>
                    <StackPanel>
                        <TextBlock Text="Today" Foreground="#AAAAAA" FontSize="12"/>
                        <TextBlock x:Name="txtTodayMinutes" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Last 7 Days Chart (Simple List for now) -->
        <GroupBox Grid.Row="1" Header="Last 7 Days" Foreground="White" Margin="0,0,0,15">
            <ListView x:Name="lstWeeklyStats" Height="150">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Date}"/>
                        <GridViewColumn Header="Pomodoros" Width="100" DisplayMemberBinding="{Binding Pomodoros}"/>
                        <GridViewColumn Header="Minutes" Width="100" DisplayMemberBinding="{Binding Minutes}"/>
                        <GridViewColumn Header="Progress" Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <ProgressBar Value="{Binding Minutes}" Maximum="120" Height="20" Foreground="#4A148C" Background="#333333"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- All Sessions History -->
        <GroupBox Grid.Row="2" Header="Session History" Foreground="White">
            <ListView x:Name="lstAllSessions">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Date &amp; Time" Width="150" DisplayMemberBinding="{Binding Date}"/>
                        <GridViewColumn Header="Duration" Width="100" DisplayMemberBinding="{Binding Duration}"/>
                        <GridViewColumn Header="Pomodoros" Width="100" DisplayMemberBinding="{Binding PomodoroCount}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</Window>